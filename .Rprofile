# .Rprofile

# for data wrangling, etc.
library(tidyverse); library(reshape2)

# for plotting
library(gridExtra); library(scales); library(ggpubr); library(ggsci); library(RColorBrewer); library(sjPlot)

# for Bayesian regression models
library(rstan); library(brms); library(shinystan)

# for random forests
library(party); library(edarf)

# for modelling distances
library(analogue) # for fusing matrices
library(phangorn) # for neighborNets

# for vadis
library(VADIS)
